import React from 'react'

const Login = (props) => {
    // const {email, setEmail, password, setPassword, handleLogin, handleSignup, hasAccount, setHasAccount, emailError, passwordError}

    return (
        <section className='login'>
            
        </section>
    )
}

export default Login
